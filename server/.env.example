# Database (SQLite for dev; use PostgreSQL URL for production)
DATABASE_URL="file:./dev.db"

# Server
PORT=4000
NODE_ENV=development

# Optional: comma-separated origins for CORS (e.g. https://yourdomain.com)
# CORS_ORIGIN=
